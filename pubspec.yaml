name: cx_calendar_appbar
description: Flutter package for custom AppBar with full calendar view with many new features!
version: 0.0.14
repository: https://github.com/caturix-app/calendar_appbar

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.1
  scrollable_positioned_list: 0.3.8

dev_dependencies:
  flutter_test:
    sdk: flutter
